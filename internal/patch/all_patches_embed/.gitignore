# The base-patch JSON is committed so the binary builds out-of-box; it's
# regenerated by `php artisan alttp:updatebuildrecord` (run from
# vendor/php-randomizer) and the new file must replace this one. Other
# patch JSONs (per-Build variants you might generate locally) are ignored.
*.json
!edc01f3db798ae4dfe21101311598d44.json
!.gitignore
